server:
  port: 8088
spring:
  #cloud:
    #config:
      #server:
        #git:
          #uri: https://github.com/prasath116/university-micro-service/tree/master/university-micro-service/config-service-repo
  profiles:
    active: native
  application:
    name: config-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/